# v2.6.1 代码审查修复记录

本轮复核修复了以下问题：

- 修正序章到星座玩法的断链，并将第 6 日星轨玩法接回第 7 日玩法链，确保 d6-d15 主线可达。
- 读档恢复变量、历史文本、访问计数、循环次数、周目数和当前场景，避免读档后条件分支和流程图状态丢失。
- 增加存档槽位边界校验，拒绝损坏的槽位和结局 JSON，并在本地存储写入失败时回滚内存状态。
- 清理迷你游戏遮罩和异步回调，返回标题或读档时不会残留浮层或继续推进旧剧情。
- 修正场景缺失时的回退行为，并补齐新增剧情场景的背景样式。
- 统一版本信息为 v2.6.1，补充生成文件、构建目录和环境变量的忽略规则。

验证包括：JavaScript 语法检查、剧情节点引用和关键链路可达性检查、存档失败回滚契约测试，以及本地 HTTP 静态资源 smoke test。

## v2.6.2 复核补丁

- 所有玩法记录读取器统一校验对象/数组结构，合法但错误类型的本地存储数据会回退到空状态。
- 回声确认写入失败时恢复原值，不再误报成功。
